# Commands, functions and aliases

# Pager default
set -gx PAGER less

# Aliases
alias lg lazygit
alias dev bin/dev
alias until_failure ~/scripts/until_failure
alias ls 'lsd'
alias tree 'lsd --tree -a -I .git'
